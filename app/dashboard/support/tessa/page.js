export default function TessaStaticPage() {
  return (
    <div style={{ padding: '2rem', background: '#fff', color: '#000' }}>
      <h1>✅ Tessa Static Test</h1>
      <p>If you see this, the page loads correctly. The crash is in the client code.</p>
    </div>
  );
}
