'use client'

// DEPRECATED — bottom nav now renders once from app/dashboard/layout.js via
// components/BottomNav.jsx. This stub exists only so pages still importing
// { Navigation } don't break the build. Safe to leave indefinitely; remove
// the import + <Navigation /> line from a page whenever you touch it next,
// no rush.
export function Navigation() {
  return null
}
